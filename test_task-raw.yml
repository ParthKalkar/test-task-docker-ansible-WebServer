---
- name : For automating the bootstrapping of the current webservice
  hosts : all

  tasks:
  
    - name: Create Network
      community.docker.container:
        name: flask_json
    
       
        
    - name : deploy flask_json
      community.docker.container:
        name: flask_json
        image: flask_json:latest
        ports:
          - "80:80"
      
